// import axios from 'axios';

// // ===================================================================
// // IMPORTANT: Replace this URL with your actual backend API URL.
// // It's best to use environment variables for this.
// // For a Vite project, you would use `import.meta.env.VITE_API_URL`.
// // ===================================================================
// const API_BASE_URL = 'http://localhost:3000/api/v1'; // <-- CHANGE THIS

// const apiClient = axios.create({
//   baseURL: API_BASE_URL,
//   headers: {
//     'Content-Type': 'application/json',
//   },
// });

// // You can add interceptors here for handling auth tokens or global errors.
// // For example, to add a token from localStorage to every request:
// // apiClient.interceptors.request.use(config => {
// //   const token = localStorage.getItem('authToken');
// //   if (token) {
// //     config.headers.Authorization = `Bearer ${token}`;
// //   }
// //   return config;
// // });

// export default apiClient;
